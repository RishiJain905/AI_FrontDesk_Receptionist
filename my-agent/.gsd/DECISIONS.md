# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? |
|---|------|-------|----------|--------|-----------|------------|
| D001 | M001 | arch | Language | Python | Scaffold is Python; LiveKit Agents SDK is Python-first; spec TS type notation is translated to Python dataclasses | No |
| D002 | M001 | arch | Module layout | Flat modules under `src/` (no subdirectory packages in initial approach) with explicit imports | Simpler than nested packages for a single-milestone build; agent.py entrypoint requirement means src/ is already on path | Yes — if codebase grows large |
| D003 | M001 | arch | Slot-filling mechanism | `AgentTask` subclass with `@function_tool` decorators for slot recording; one tool per slot or grouped by intent | LiveKit AgentTask is the idiomatic pattern for discrete collection tasks; function tools give LLM clear structured extraction targets | Yes — revisit if tool-call reliability is poor |
| D004 | M001 | arch | Safety branch | `session.update_agent(SafetyAgent())` via function tool trigger when LiveClassifier detects danger | Idiomatic LiveKit handoff; keeps safety logic in a separate minimal agent with no intake tools | No |
| D005 | M001 | arch | Timezone handling | Python stdlib `zoneinfo` (Python 3.10+) | Project requires Python >=3.10; zoneinfo is stdlib, no extra dep needed | Yes — if pytz compatibility needed |
| D006 | M001 | arch | HTTP client for integrations | `httpx` with async client | Async-first; fits LiveKit's async event loop; cleaner than aiohttp for REST | Yes — if aiohttp already in deps |
| D007 | M001 | arch | SMS provider | Twilio as default; abstracted behind AlertService protocol | Most common; good Python client; spec does not mandate a provider; protocol abstraction allows swap | Yes — if provider preference changes |
| D008 | M001 | convention | Config types | Python dataclasses (not Pydantic) for `BusinessConfig` and `CallIntakeRecord` | Keeps deps minimal; Pydantic not in current pyproject.toml; dataclasses sufficient for this scale | Yes — if validation complexity grows |
| D009 | M001 | arch | Background classification timing | Live classifier runs synchronously in conversation_item_added event handler (lightweight keyword check); final classifier runs as background asyncio task post-call using LLM | Live check must be <10ms to not block; LLM-based final classification can be async post-call | No |
| D010 | M001 | convention | CRM write timing | Async post-call, in a background task fired from call lifecycle finalize(); does not block session teardown | CRM latency must not affect voice session quality | No |
| D011 | M001 | convention | Duplicate alert prevention | Track `sms_sent` boolean on `CallIntakeRecord` / lifecycle state; check before send | Simple, sufficient for V1 single-call scope | Yes — if multi-worker deployment needs distributed lock |
| D012 | M001 | scope | GoHighLevel API version | v2 API (`https://services.leadconnectorhq.com`) with Authorization: Bearer token | GHL v1 is deprecated; v2 is current; Bearer token auth is standard | No |
| D013 | M001 | convention | Type module names | `src/hvac_types/` and `src/config/` as subdirectories | Subdirectories map better to the spec boundaries; naming `hvac_types` instead of `types` avoids shadowing the Python stdlib `types` module. | No |
| D014 | S01/T01 | convention | str Enum __str__ | All `str, Enum` subclasses override `__str__` to `return self.value` | Python 3.11+ changed `str(StrEnum.MEMBER)` from returning the value to `'ClassName.MEMBER'`; project runs 3.13.2; override restores expected behaviour across all versions and ensures enum values match LLM output tokens directly | No |
| D015 | S01/T03 | convention | Config boot validation | `load_config()` performs eager required-field validation (`business_name`, `timezone`, `owner_phone`) with whitespace-aware non-empty string checks before runtime use | Fails fast on misconfiguration during initialization so downstream call/dispatch flows never start with invalid core business identity/contact context | Yes — revisit if config schema migrates to a dedicated validation library |
| D016 | S02/T01 | convention | Proof-test import boundary | S02 proof tests import planned `conversation.*` and `classification.*` modules directly instead of hidden placeholders | Keeps RED state honest by surfacing missing runtime modules and public contracts immediately during collection | No |
| D017 | S02/T02 | arch | Slot semantics normalization | Preserve S01 `SlotStatus.EMPTY/FILLED/CONFIRMED` as the storage contract and expose S02 `missing/tentative/confirmed` semantics through `SlotTracker` accessors | Avoids churn in shipped type contracts while giving downstream slices stable inspection helpers that match slice requirements and tests | No |
| D018 | S02/T03 | arch | IntakeTask turn controller | Inject deterministic per-turn tool recommendations and reply requirements into `IntakeTask` instructions, including a hard rule that same-turn tentative values cannot be self-confirmed | Makes the LiveKit evals stable by keeping structured state updates tool-driven while preventing the model from prematurely confirming or completing intake | Yes — revisit if a lower-level guard becomes available in the SDK |
| D019 | S02/T04 | convention | Test-time env loading | Pytest loads `.env.local` first and then `.env` via `tests/conftest.py` before constructing LiveKit inference models | Lets slice verification run with securely collected local secrets without changing the named verification command or depending on shell-specific env export behavior | Yes — revisit if test infrastructure moves to explicit fixture injection or CI-managed env setup |
| D020 | S03/plan | verification | Safety branch proof strategy | S03 verification must assert a real LiveKit `agent_handoff` event plus safety-response intent, not prose-only safety wording | This gives direct proof that the runtime switched agents immediately on danger, matching the roadmap proof strategy and avoiding false confidence from prompt-only behavior | No |
| D021 | S03/plan | observability | Controller diagnostics surface | S03 controller should expose inspectable current mode, latest live classification, handoff reason, and completed-intake summary state for tests and future debugging | Keeps safety-branch and close-behavior failures localizable to prompt composition vs classification vs handoff wiring without relying on ad hoc logs | Yes — revisit if S05 introduces a richer lifecycle state surface |
| D022 | S03/T02 | arch | Safety handoff implementation shape | LiveKit handoff events are only emitted reliably when a function tool returns the replacement `Agent`; direct `session.update_agent(...)` inside controller hooks changed behavior but did not surface an eval-visible `agent_handoff` event within the same run | Matches observed SDK behavior in `agent_activity`/`generation` and explains why orchestration should be separated from inherited intake tools on the normal controller | Yes — revisit if SDK lifecycle hooks change or a documented controller-side handoff path becomes available |
| D023 | S03/T03 | arch | Turn-time controller routing | `HVACConversationController` must enforce greeting-only and danger routing in `llm_node`, not only `on_user_turn_completed`, because `AgentSession.run(...)` drives eval/console text turns through `generate_reply()` and bypasses `on_user_turn_completed` | Keeps console/eval behavior aligned with live turns, prevents greeting-only tool pollution, and allows handoff-only tool exposure before the first LLM decision on danger turns | Yes — revisit if future LiveKit session APIs route text runs through `on_user_turn_completed` |
| D024 | S03/T03 | arch | Runtime agent factory separation | `src/agent.py` exposes `build_runtime_agent()` to load validated config and construct `HVACConversationController`, and `session.start(...)` consumes that factory instead of inlining controller creation | Preserves the starter bootstrap shape, isolates config/controller composition for later S05 lifecycle wiring, and makes entrypoint regression checks straightforward | Yes — revisit if later slices require a richer runtime builder signature |
| D025 | S04/plan | verification | Integration proof boundary | S04 verification uses `httpx.MockTransport` pytest coverage to assert exact GoHighLevel/Twilio request shapes plus typed failure diagnostics; live-credential proof is deferred to later operational/UAT work | Gives deterministic CI-proof for R005/R006 while honoring D006's async transport choice and avoiding flaky external-network requirements during the slice | Yes — revisit if the milestone later adds a live sandbox verification gate |
| D026 | S04/plan | observability | Integration error surface | `CrmError` and `SmsError` should expose stable `service` / `operation` / `status_code` metadata while redacting auth secrets and oversized transcript payloads from surfaced messages | Supports R009 failure isolation and keeps future retries/debugging localizable without leaking sensitive data | Yes — revisit if a shared cross-slice error envelope replaces per-service errors |
| D027 | S04/plan | arch | Integration record enrichment | Extend `CallIntakeRecord` in S04 with optional `callback_time`, `customer_type`, `transcript`, `ai_summary`, `notify_owner`, and `sms_sent` fields instead of passing ad hoc side-channel data into CRM/SMS mappers | The CRM note and SMS alert requirements cannot be met cleanly with the current S01 record shape; enriching the shared record keeps S05 lifecycle wiring simple and typed | Yes — revisit if a separate finalized-call DTO is introduced later |
| D028 | S04/T02 | convention | Shared phone validation contract | `normalize_phone()` returns normalized E.164 output for supported North American inputs and `None` for unusable values; provider layers are responsible for converting missing/invalid numbers into typed `CrmError`/`SmsError` failures when the operation cannot proceed | Matches the RED utility contract, keeps normalization reusable for match/skip logic, and avoids forcing lower-level utils to know provider-specific failure semantics | Yes — revisit if later slices require a dedicated typed validation error at the utility boundary |
| D029 | S04/T03 | arch | GoHighLevel duplicate-match rule | `GoHighLevelService` must treat GoHighLevel search results as candidates only and decide create vs update solely by exact shared-phone normalization equality before attaching a deterministic structured note | The GoHighLevel search endpoint is fuzzy; exact normalized matching prevents accidental updates to unrelated contacts while keeping request order and note content inspectable in tests and retries | No |
| D030 | S04/T04 | arch | Twilio alert boundary | `TwilioSmsService` normalizes owner and sender numbers before posting `Messages.json`, skips outbound requests when `notify_owner` is false or `sms_sent` is true, and builds the owner-facing SMS from the finalized record with a concise summary-first message plus an issue/danger fallback when no explicit summary exists | Keeps duplicate prevention and transport validation inside the provider boundary, preserves the single shared phone-normalization path, and gives S05 a stable async alert service without importing Twilio or CRM details | Yes — revisit if later slices introduce richer alert templating or multi-recipient escalation |
| D031 | S05/plan | verification | Lifecycle proof boundary | S05 verification uses pure/unit pytest seams (`tests/test_after_hours_gate.py`, `tests/test_call_lifecycle.py`, `tests/test_agent.py`, `tests/test_types.py`) to prove overnight gate math, finalize-once behavior, caller-ID fallback, provider isolation, and real entrypoint composition before any live telephony/UAT work | Gives operational proof for R007/R009 and supporting proof for R005 without depending on flaky live SIP or external-network execution in the slice gate | Yes — revisit if later milestone gates require live telephony proof inside CI |
| D032 | S05/plan | arch | Fallback-phone and hours contract | Extend `BusinessConfig` with explicit after-hours window fields and extend `CallIntakeRecord` with caller-ID plus callback-confirmation state instead of encoding those distinctions in notes or hardcoded lifecycle rules | R007 must be config-driven per R008, and R009's caller-ID fallback cannot be represented honestly with the current shared record contract | Yes — revisit if a separate finalized-call DTO or config schema is introduced later |
| D033 | S05/plan | observability | Lifecycle finalization diagnostics | `CallLifecycle` should expose a stable snapshot/status surface and structured logger phases for gate, transcript, finalize, CRM, and SMS outcomes, reusing redacted `IntegrationError.to_dict()` metadata | Keeps partial-call, duplicate-finalize, and downstream-provider failures localizable without leaking secrets or raw transcript payloads | Yes — revisit if a broader application observability framework supersedes slice-local lifecycle diagnostics |
| D034 | S05/T02 | arch | Timezone fallback behavior for injected gate tests | Keep `zoneinfo` as primary timezone resolver, and when tzdata is unavailable in Windows-like environments, allow `is_after_hours(..., now=...)` to fall back to a deterministic Toronto DST conversion for the demo timezone while still raising a typed `TimezoneResolutionError` for unresolved zones | Preserves D005's stdlib-first contract, keeps deterministic after-hours boundary tests runnable in environments missing tzdata, and still surfaces actionable errors when runtime clock resolution needs real timezone data | Yes — revisit if tzdata is added as a guaranteed runtime dependency |
| D035 | S05/T03 | arch | Lifecycle finalization source-of-truth | `CallLifecycle` finalization now consumes committed `conversation_item_added` events via `TranscriptAssembler` and controller diagnostics via `FinalClassifier`, while snapshot/log surfaces expose redacted provider outcomes and hide raw transcript bodies | Keeps post-call orchestration deterministic, idempotent, and inspectable without leaking secrets or requiring call replay | Yes — revisit if a centralized observability framework supersedes slice-local lifecycle snapshots |
| D036 | S05/T04 | arch | Entrypoint integration-provider fallback | `src/agent.py` now builds real GoHighLevel/Twilio providers when required env vars are present and otherwise composes no-op protocol-compatible fallbacks while still wiring `CallLifecycle` and diagnostics | Keeps the runtime entrypoint operational and observable in local/dev environments without credentials, while preserving real provider execution paths for configured deployments | Yes — revisit in S06 if missing-provider startup should hard-fail instead of graceful fallback |
